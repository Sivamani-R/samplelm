# NexLeave Enterprise - Environment Configuration
# Set VITE_API_BASE_URL to point to your live Node.js/Express backend
VITE_API_BASE_URL=http://localhost:5000/api

# Set to 'false' when connecting to live backend, 'true' to use the decoupled mock backend service
VITE_USE_MOCK=true
